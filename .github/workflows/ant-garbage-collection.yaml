name: ANT Garbage Collection

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 1-7 * 1' # Run on the first Monday of each month at midnight UTC

jobs:
  garbage-collection:
    environment: ${{ github.ref_name}}
    permissions:
      contents: read
      actions: read
    runs-on: ubuntu-latest
    env:
      REGISTRY_ID: ${{ vars.REGISTRY_ID}}
      WALLET: ${{ secrets.WALLET }}
      VAOT_ID: ${{ vars.VAOT_ID}}
      CU_URL: ${{ vars.CU_URL}}
      GRAPHQL_URL: ${{ vars.GRAPHQL_URL}}
      ARIO_PROCESS_ID: ${{ vars.ARIO_PROCESS_ID}}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - name: Setup
        run: yarn

      - name: Run Ant Garbage Collection
        run: |
          GARBAGE_COLLECTION_OUTPUT=$(yarn ant-garbage-collection 2>&1)
          echo "$GARBAGE_COLLECTION_OUTPUT"
          # Extract proposal numbers and message IDs from the output
          PROPOSALS=$(echo "$GARBAGE_COLLECTION_OUTPUT" | grep "Proposal [0-9]" | sed 's/^[[:space:]]*//' || echo "No proposals found")
          echo "::set-output name=proposals::$PROPOSALS"
        id: ant-garbage-collection

      - name: Notify Failure
        if: failure()
        uses: rtCamp/action-slack-notify@v2.3.0
        env:
          SLACK_COLOR: ${{ job.status }}
          SLACK_TITLE: ANT Garbage Collection Encountered an Error!
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_CUSTOM_PAYLOAD: |
            {
              "attachments": [
                {
                  "fallback": "ANT Garbage Collection Failed",
                  "color": "danger",
                  "title": "Details",
                  "text": "The ANT Garbage Collection process has failed!",
                  "fields": [
                    {
                      "title": "Network",
                      "value": "${{ github.ref_name }}",
                      "short": true
                    },
                    {
                      "title": "Process ID",
                      "value": "${{ vars.REGISTRY_ID }}",
                      "short": true
                    },
                                      {
                    "title": "Proposals Created",
                    "value": "${{ steps.ant-garbage-collection.outputs.proposals || 'None created before failure' }}",
                    "short": false
                  },
                  {
                    "title": "Vote on proposals in VAOT",
                    "value": "https://vaot.ar.io/#/${{ vars.VAOT_ID }}",
                    "short": false
                  },
                  {
                    "title": "GitHub Action",
                    "value": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                    "short": false
                  }
                  ]
                }
              ]
            }

      # on success send a slack message
      - name: Notify Success
        if: success()
        uses: rtCamp/action-slack-notify@v2.3.0
        env:
          SLACK_COLOR: ${{ job.status }}
          SLACK_TITLE: ANT Garbage Collection Success!
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_CUSTOM_PAYLOAD: |
            {
              "attachments": [{
                "fallback": "ANT Garbage Collection Success!",
                "color": "good",
                "title": "Collection Results",
                "text": "The ANT Garbage Collection process has completed successfully!",
                "fields": [
                  {
                      "title": "Network",
                      "value": "${{ github.ref_name }}",
                      "short": true
                    },
                  {
                    "title": "Process ID",
                    "value": "${{ vars.REGISTRY_ID }}",
                    "short": true
                  },
                  {
                    "title": "Proposals Created",
                    "value": "${{ steps.ant-garbage-collection.outputs.proposals || 'No proposals created' }}",
                    "short": false
                  },
                  {
                    "title": "Vote on proposals in VAOT",
                    "value": "https://vaot.ar.io/#/${{ vars.VAOT_ID }}",
                    "short": false
                  },
                  {
                    "title": "GitHub Action",
                    "value": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                    "short": false
                  }
                ]
              }]
            }
